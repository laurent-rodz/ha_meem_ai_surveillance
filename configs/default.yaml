# Default system settings

pipeline:
  device: cuda
  precision: fp16
  batch_size: 1

logging:
  level: INFO

models:
  scrfd_onnx:  E:\ha_meem_ai_surveillance\models\exported\scrfd_10g_bnkps.onnx
  adaface_onnx:  E:\ha_meem_ai_surveillance\models\exported\adaface.onnx